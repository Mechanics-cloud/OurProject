type UsePaginationParamType = {
  count: number
  currentPage?: number
  onChange: (pageNumber: number) => void
  page: number
}

// export const usePagination = ({count, currentPage, currentPage, page}: UsePaginationParamType) => {
//   return {}
// }
